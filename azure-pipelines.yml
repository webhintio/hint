# These are the different types of jobs run by Azure Pipelines:
#
# * Generic PRs
# * Changes in master
# * Dependabot PRs
# * Cron jobs to do a full analysis
jobs:

# Generic PRs
- template: azure-pipelines/prs.yml
  parameters:
    name: Linux_PR
    vmImage: 'ubuntu 16.04'

- template: azure-pipelines/prs.yml
  parameters:
    name: macOS_PR
    vmImage: 'macOS-10.13'

- template: azure-pipelines/prs.yml
  parameters:
    name: Windows_PR
    vmImage: 'vs2017-win2016'
