<%- include('../partials/header', { analyze: { text: 'Analyze again' } }); %>
<section class="results page">
    <h1 class="page__header">Hints</h1>
    <% categories.forEach(function (category) { %>
        <%- include('../partials/category', category); %>
    <% }); %>
</section>
