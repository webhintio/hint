<div>
    <span><%= category %></span>
    <% problems.forEach(function (problem) { %>
        <% if (problem.category === category) { %>
            <%- include('result-hint', { problem: problem }); %>
        <% } %>
    <% }); %>
</div>
