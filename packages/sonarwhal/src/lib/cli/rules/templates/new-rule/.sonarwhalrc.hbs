{
    "connector": {
        "name": "jsdom",
        "options": {
            "waitFor": 1000
        }
    },
    "formatters": "summary",
    "rulesTimeout": 120000,
    "rules": {
        {{#each rules}}
        "{{#if ../isMulti}}{{../normalizedName}}/{{/if}}{{normalizedName}}": "warning"{{#unless @last}},{{/unless}}
        {{/each}}
    }
}
