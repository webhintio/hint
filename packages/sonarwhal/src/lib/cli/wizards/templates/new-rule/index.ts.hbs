/**
 * @fileoverview {{description}}
 */

{{#each rules}}
import * as {{toCamelCase normalizedName}} from './{{normalizedName}}';
{{/each}}

module.exports = {
    {{#each rules}}
    '{{normalizedName}}': {{toCamelCase normalizedName}}{{#unless @last}},{{/unless}}
    {{/each}}
};
