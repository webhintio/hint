import builder from '../../../src/lib/collectors/jsdom/jsdom';
import { testCollector } from './_common';

// First test that it passes the standard tests for all collectors
testCollector(builder);

// Specific tests go here
